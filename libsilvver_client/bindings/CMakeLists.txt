SET(BINDINGS "C++")

INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/src)

ADD_SUBDIRECTORY(python)

MESSAGE("")
MESSAGE("Language bindings:")
FOREACH(LANGUAGE ${BINDINGS})
  MESSAGE("  * " ${LANGUAGE})
ENDFOREACH(LANGUAGE)
MESSAGE("")
